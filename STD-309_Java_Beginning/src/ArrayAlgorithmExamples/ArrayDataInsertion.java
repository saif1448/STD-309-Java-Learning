package ArrayAlgorithmExamples;

public class ArrayDataInsertion {
    public static void main(String[] args) {
        //[10, 20, 30, 40, 0] ---> 5 length
        // insert a data after 20
        // 20 ---> index = 1
        // you have to insert data after index 1
        // we are gonna insert 25 after index 1
        // for(i = index+1; i < arr.length; i++){
        // 30 ----> 2 == i = 2
        // i+1 = 3, arr[3] = 30,  if i + 1 is in the range
        //
        // if(i+1 < arr.length) {
        // next_data = arr[i+1]
        // arr[i+1] = arr[i]
        // do this repetatively
        // [10, 20, 25, 30, 40]


    }
}
